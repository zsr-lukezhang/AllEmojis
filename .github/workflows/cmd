name: Infinite CMD Loop

on:
  workflow_dispatch:

jobs:
  infinite-loop:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Infinite loop to open CMD
      run: |
        :loop
        start cmd.exe
        goto loop
